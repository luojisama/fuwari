
---

import MainGridLayout from "../layouts/MainGridLayout.astro";

import { getEntry } from "astro:content";
import { render } from "astro:content";
import Comment from "@components/Comment.astro";
import Markdown from "@components/misc/Markdown.astro";
import { Icon } from "astro-icon/components";
import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";

const friendsPost = await getEntry("spec", "friends");

if (!friendsPost) {
	throw new Error("Friend page content not found");
}

const { Content } = await render(friendsPost);

const myFriendLinkItem = {
	title: "白咲のBLOG",
	imgurl: "https://blog.shiro.team/avatar.png",
	desc: "歇斯底里是崩溃，底里歇斯是美味.",
	siteurl: "https://blog.shiro.team",
	tags: [" "],
};

const myFriendLink = `{
	title: "${myFriendLinkItem.title}",
	imgurl: "${myFriendLinkItem.imgurl}",
	desc: "${myFriendLinkItem.desc}",
	siteurl: "${myFriendLinkItem.siteurl}",
	tags: [" "],
},`;

const items = [
	{
		title: "Astro",
		imgurl: "https://avatars.githubusercontent.com/u/44914786?s=48&v=4",
		desc: "The web framework for content-driven websites. ⭐️ Star to support our work!",
		siteurl: "https://github.com/withastro/astro",
		tags: [" "],
	},
	{
		title: "时歌的博客",
		imgurl: "https://www.lapis.cafe/avatar.webp",
		desc: "理解以真实为本，但真实本身并不会自动呈现",
		siteurl: "https://www.lapis.cafe",
		tags: [" "],
	},
	{
		title: "𝙕𝙚𝙡𝙡𝙤𝙣的博客",
		imgurl: "https://www.zellon.top/avatar.jpg",
		desc: "告别过去，是为了走向未来",
		siteurl: "https://www.zellon.top",
		tags: [" "],
	},
	{
		title: "mikann_OWQ",
		imgurl: "https://mikann.fun/avatar.webp",
		desc: "是一位超级无敌可爱美少女",
		siteurl: "https://www.mikann.fun",
		tags: [" "],
	},
	{
		title: "Viki 写东西的地方",
		imgurl: "https://blog.viki.moe/avatar.png",
		desc: "生活需要记录。",
		siteurl: "https://blog.viki.moe",
		tags: [" "],
	},
];
---
<MainGridLayout title={i18n(I18nKey.friends)} description={i18n(I18nKey.friends)}>
    <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32">
        <div class="card-base z-10 px-9 py-6 relative w-full ">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-6 gap-y-8 my-4">
                <div class="flex flex-nowrap items-stretch h-28 gap-4 rounded-[var(--radius-large)]" data-copy-friend-link-card>
                    <div class="w-28 h-28 flex-shrink-0 rounded-lg overflow-hidden bg-zinc-200 dark:bg-zinc-900">
                        <img src={myFriendLinkItem.imgurl} alt="站点头像" class="w-full h-full object-cover">
                    </div>
                    <div class="grow w-full">
                        <div class="font-bold transition text-lg text-neutral-900 dark:text-neutral-100 mb-1">{myFriendLinkItem.title}</div>
                        <div class="text-50 text-sm font-medium">{myFriendLinkItem.desc}</div>
                        <div class:list={["items-center", {"flex": true, "hidden md:flex" : false}]}>
                            <div class="flex flex-row flex-nowrap items-center">
                                <span class="transition text-50 text-sm font-medium" data-copy-status aria-live="polite">我的友链</span>
                            </div>
                        </div>
                    </div>
                    <button
                        id="copy-my-friend-link"
                        type="button"
                        data-copy={myFriendLink}
                        data-label="一键复制我的友链"
                        data-status-label="我的友链"
                        data-success-label="已复制"
                        data-failure-label="复制失败"
                        data-copy-friend-link-button
                        aria-label="一键复制我的友链"
                        title="一键复制我的友链"
                        class="flex btn-regular w-[3.25rem] flex-shrink-0 rounded-lg bg-[var(--enter-btn-bg)] hover:bg-[var(--enter-btn-bg-hover)] active:bg-[var(--enter-btn-bg-active)] active:scale-95 disabled:pointer-events-none disabled:opacity-70"
                    >
                        <Icon name="material-symbols:content-copy-outline-rounded" class="transition text-[var(--primary)] text-3xl mx-auto" data-copy-icon></Icon>
                        <Icon name="material-symbols:check-rounded" class="hidden transition text-[var(--primary)] text-3xl mx-auto" data-success-icon></Icon>
                    </button>
                </div>
                {items.map((item) => (   
                    <div class="flex flex-nowrap items-stretch h-28 gap-4 rounded-[var(--radius-large)]">
                        <div class="w-28 h-28 flex-shrink-0 rounded-lg overflow-hidden bg-zinc-200 dark:bg-zinc-900">
                            <img src={item.imgurl} alt="站点头像" class="w-full h-full object-cover">
                        </div>
                        <div class="grow w-full">
                            <div class="font-bold transition text-lg text-neutral-900 dark:text-neutral-100 mb-1">{item.title}</div>
                            <div class="text-50 text-sm font-medium">{item.desc}</div>
                            <div class:list={["items-center", {"flex": true, "hidden md:flex" : false}]}>
                                <div class="flex flex-row flex-nowrap items-center">
                                    {(item.tags && item.tags.length > 0) && item.tags.map((tag,i) => (  
                                    <div class:list={[{"hidden": i==0}, "mx-1.5 text-[var(--meta-divider)] text-sm" ]}>
                                        /
                                    </div>  
                                    <span class="transition text-50 text-sm font-medium">
                                        {tag}
                                    </span>))}
                                    {!(item.tags && item.tags.length > 0) && <div class="transition text-50 text-sm font-medium">{i18n(I18nKey.noTags)}</div>}
                                </div>
                            </div>
                        </div>
                        <a href={item.siteurl} target="_blank" rel="noopener noreferrer"class="flex btn-regular w-[3.25rem] rounded-lg bg-[var(--enter-btn-bg)] hover:bg-[var(--enter-btn-bg-hover)] active:bg-[var(--enter-btn-bg-active)] active:scale-95">
                            <svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="transition text-[var(--primary)] text-4xl mx-auto iconify iconify--material-symbols" width="1em" height="1em" viewBox="0 0 24 24">
                                <path fill="currentColor" d="M12.6 12L8.7 8.1q-.275-.275-.275-.7t.275-.7t.7-.275t.7.275l4.6 4.6q.15.15.213.325t.062.375t-.062.375t-.213.325l-4.6 4.6q-.275.275-.7.275t-.7-.275t-.275-.7t.275-.7z"></path>
                            </svg>
                        </a>
                    </div>
                ))}
            </div>  
            <Markdown class="mt-2">
                <Content />
            </Markdown>
        </div>
    </div>

    <Comment />
</MainGridLayout>
